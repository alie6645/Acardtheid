import java.awt.*;
import java.util.ArrayList;
import java.util.List;

public class Board {
    List<Card> active = new ArrayList<>();
    List<Card> fate = new ArrayList<>();

    public void draw(Graphics2D g2){
        for (Card card:active){
            if (card.type.equals("anc")){

            }
        }
    }
}
